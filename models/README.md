# Model files

Model weights are intentionally kept outside Git.

For each model, record the following in a small Markdown or YAML file:

- upstream repository and filename
- pinned revision
- file size
- SHA-256 checksum
- upstream license
- runtime and hardware compatibility notes

Large files belong under the local path configured by `MODEL_DIR`, not in this repository.
