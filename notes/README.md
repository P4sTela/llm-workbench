# Notes

Keep one short note per meaningful run. Include the date, machine/GPU, model revision, runtime commit or image, launch options, and measured results.

Results that have not been run on hardware should be labelled as proposals rather than benchmarks.
